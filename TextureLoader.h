/*#pragma once

#include "Win32OpenGL.h"

class TextureLoader
{

private:

	// no objects - only static methods

	TextureLoader();
	~TextureLoader();

public:

	static int LoadBMP(const string location, GLuint &texture); // TEST LATER!!! const char* location

};*/

